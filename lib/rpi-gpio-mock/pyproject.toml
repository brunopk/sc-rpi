[project]
name = "rpi-gpio-mock"
version = "0.7.1"
description = ""
readme = "README.md"
authors = [
    { name = "Bruno Piaggio", email = "brunopiaggiok@gmail.com" }
]
requires-python = "==3.9.*"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/RPi"]