[project]
name = "rpi-ws281x-mock"
version = "5.0.0"
description = ""
readme = "README.md"
authors = [
    { name = "Bruno Piaggio", email = "brunopiaggiok@gmail.com" }
]
requires-python = "==3.9.*"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rpi_ws281x"]