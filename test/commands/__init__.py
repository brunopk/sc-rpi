"""Tests for modules in `sc_rpi.commands` package."""

# TODO: CONTINUE consider removing add_section command (sections will be added once and then handled from home assistant just to invoke edit_section) maybe, save created sections (this will change the way to test commands)