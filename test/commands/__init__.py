"""Tests for modules in `sc_rpi.commands` package."""

# TODO: CONTINUE test all commands but parsing from JSON (string), remove commands packages when there's only one module inside (such as commands/help/)
# TODO: consider removing add_section command (sections will be added once and then handled from home assistant just to invoke edit_section) maybe, save created sections (this will change the way to test commands)